- defaults:
    name: nothing

- job-template:
    name: '{folder_name}'
    defaults: nothing
    project-type: folder

- project:
    name: folders
    jobs:
      - '{folder_name}':
          folder_name:
              - 'topoapps-snmp'

